---
name: korean-technical-writing
description: Write, revise, and review Korean technical prose for developers and technical readers. Preserve technical facts, identifiers, measurements, uncertainty, and causal claims while making Korean clearer, more precise, less ambiguous, and easier to scan. Use for engineering logs, READMEs, architecture notes, incident reports, release notes, technical articles, troubleshooting guides, design documents, and explanatory developer documentation.
---

# Korean Technical Writing

기술적 의미를 바꾸지 않으면서, 개발자가 처음 읽어도 한 번에 이해할 수 있는 자연스럽고 모호하지 않은 한국어로 쓴다.

이 스킬은 문장을 더 그럴듯하게 만드는 것이 목적이 아니다. **정확성, 검증 가능성, 의미 보존, 독해 비용 감소**가 목적이다.

## 우선순위

다음 순서를 항상 지킨다.

1. 사실과 검증 수준 보존
2. 독자와 문서 목적에 맞는 구조
3. 모호성 제거
4. 평이하고 정확한 한국어
5. 문장 리듬과 표현 다듬기

하위 단계의 개선이 상위 단계를 손상하면 적용하지 않는다.

## 작업 절차

### 1. 보존해야 할 사실을 먼저 고정한다

본문을 고치기 전에 다음 항목을 찾는다.

- 숫자, 단위, 날짜, 시간, 버전
- 코드 심볼, 명령어, 파일명, 경로, 옵션, 설정 키
- 오류 메시지, 로그 문자열, 프로토콜 이름
- 비교 대상과 기준선
- 조건, 예외, 부정문
- 인과관계와 상관관계
- 관찰, 추정, 가설, 검증의 구분
- 테스트 범위와 환경
- 아직 확인하지 않은 항목

자세한 규칙은 `references/preservation-contract.md`를 따른다.

### 2. 문서의 역할을 정한다

하나의 글이 무엇을 하려는지 먼저 정한다.

- **작업 안내**: 독자가 어떤 작업을 끝내도록 돕는다.
- **문제 해결**: 증상, 원인 후보, 해결, 확인 방법을 제공한다.
- **참조**: 정확한 사실을 빠르게 찾도록 한다.
- **설명**: 원리, 배경, 선택 이유, trade-off를 이해하게 한다.
- **엔지니어링 기록**: 문제, 변경, 검증, 한계를 남긴다.

한 문서 안에서 여러 역할이 필요하면 섹션을 분리한다. 역할이 충돌하면 별도 문서로 나눈다.

`references/document-shapes.md`에 기본 구조가 있다.

### 3. 구조를 먼저 고친다

문장 표현보다 먼저 다음을 확인한다.

- 핵심 결과가 너무 늦게 나오지 않는가?
- 문제, 변경, 결과, 한계가 섞여 있지 않은가?
- 한 문단이 하나의 중심 주제를 다루는가?
- 제목과 소제목만 훑어도 내용의 흐름이 보이는가?
- 독자가 알아야 할 조건이 결과 뒤에 숨어 있지 않은가?

구조가 나쁘면 문장만 매끈하게 고치지 않는다.

### 4. 모호성을 제거한다

다음 표현이 둘 이상으로 해석될 수 있는지 확인한다.

- 이것, 그것, 이 값, 이 방식, 해당
- 그리고, 또는, 및, `/`
- 만, 만약, 제외, 이외, 외
- 비교급과 배수 표현
- 생략된 주어와 목적어
- 긴 명사구
- 괄호 안의 조건

필요하면 대명사 대신 명사를 다시 쓴다. 같은 개념은 같은 이름으로 부른다.

`references/ambiguity-and-precision.md`를 따른다.

### 5. 평이하고 정확한 한국어로 쓴다

- 내부 은어와 즉흥적 은유보다 통용되는 기술 표현을 우선한다.
- 추상 명사보다 실제 동작을 나타내는 동사를 우선한다.
- 번역투와 불필요한 한자어를 줄인다.
- 행위자가 중요하거나 모호할 때만 주어를 명시한다.
- 조건은 가능한 한 해당 결과나 동작 가까이에 둔다.
- 한 문장에는 중심 생각 하나를 둔다.
- 너무 짧은 문장을 기계적으로 나열하지 않는다.
- 이미 정확하고 자연스러운 문장은 고치지 않는다.

`references/korean-style.md`를 따른다.

### 6. 프로젝트 용어집과 실제 코드를 기준으로 삼는다

**용어집이 있으면 그것이 정본이다.** 용어를 쓰기 전에 대상 프로젝트의 용어집
(`docs/GLOSSARY.md` 등)을 먼저 찾는다. 용어집에 있으면 그 표기를 쓰고 새 이름을 만들지 않는다.
필요한 개념이 용어집에 없으면 용어집에 먼저 세운 뒤 그 표기를 쓴다.

용어집 다음은 코드다. 코드에 존재하는 심볼, 설정 키, 파일명, 공식 용어가 있으면 임의의 동의어보다 우선한다.

예를 들어 코드가 `retryBudget`이라고 부르는 값을 문서에서 임의로 `재시도 한도`, `재시도 주머니`, `재시도 여유분`으로 번갈아 부르지 않는다.

다만 독자가 처음 보는 고유 용어라면 첫 등장에 짧게 뜻을 설명할 수 있다.

### 7. 마지막에 의미 보존을 다시 검토한다

수정본과 원문을 비교해 다음을 확인한다.

- 숫자나 단위가 바뀌지 않았는가?
- 조건이나 예외가 사라지지 않았는가?
- `테스트함`이 `검증됨`으로 바뀌지 않았는가?
- 추정이 사실처럼 바뀌지 않았는가?
- 상관관계가 인과관계로 바뀌지 않았는가?
- 코드 심볼이 자연어로 치환되지 않았는가?
- 문장을 줄이는 과정에서 정보가 빠지지 않았는가?
- 표현을 고친 결과가 원문보다 더 기계적이지 않은가?

`references/review-checklist.md`를 사용한다.

## 엔지니어링 기록 기본 구조

개발 일지, 실험 기록, 구현 노트에는 다음 순서를 우선한다.

1. **문제** — 무엇이 잘못됐거나 부족했는가
2. **변경** — 무엇을 어떻게 바꿨는가
3. **검증** — 어떤 환경과 방법으로 무엇을 확인했는가
4. **결과** — 수치와 관찰 결과가 무엇인가
5. **한계** — 무엇이 아직 확인되지 않았거나 해결되지 않았는가

모든 항목이 항상 필요한 것은 아니다. 없는 정보를 만들어 채우지 않는다.

## 문장 편집 원칙

### 바꿔야 하는 표현

의미가 명확해지는 경우 내부 은어와 은유를 구체적인 기술 표현으로 바꾼다.

- `배수를 걷었다` → `배수를 제거했다`
- `값을 본다` → 실제 동작에 따라 `값을 사용한다`, `비교한다`, `제한한다`
- `물리 천장` → `물리적 상한`
- `재시도 손잡이` → 실제 의미가 맞다면 `최대 재시도 횟수 설정`
- `큐에 태운다` → 실제 동작이 맞다면 `큐에 넣는다`

위 변환은 사전이 아니다. 코드와 문맥이 실제 의미를 뒷받침할 때만 적용한다.

### 바꾸지 않아야 하는 표현

다음 문장은 특별한 문제가 없으면 그대로 둔다.

> p99 지연은 240ms에서 180ms로 줄었습니다.

`지연`을 `응답 지연 시간`으로 늘리거나 `줄었습니다`를 `감소했습니다`로 치환하는 것은 개선이 아니다.

## 리뷰 모드

사용자가 리뷰만 요청하면 전체를 다시 쓰지 않는다.

다음 순서로 문제를 보고한다.

1. **사실과 의미** — 의미가 달라지거나 오해를 만들 수 있음
2. **구조** — 독자가 핵심을 찾기 어려움
3. **모호성** — 둘 이상으로 읽힐 수 있음
4. **한국어** — 번역투, 과도한 명사화, 불필요한 반복
5. **용어** — 같은 개념을 여러 이름으로 부름
6. **수치와 표** — 단위, 기준선, 측정 조건, 추정과 실측의 구분

분류 이름과 순서는 `references/review-checklist.md`의 A~F와 같다.

각 지적에는 가능한 경우 바로 고칠 수 있는 문장을 함께 제시한다.

## 참조 파일

필요한 파일만 읽는다.

- 의미와 사실 보존: `references/preservation-contract.md`
- 한국어 문장: `references/korean-style.md`
- 모호성 제거: `references/ambiguity-and-precision.md`
- 문서 구조: `references/document-shapes.md`
- 최종 검토: `references/review-checklist.md`
- 실제 예시: `references/examples.md`
- 평가 케이스: `evals/cases.md`
